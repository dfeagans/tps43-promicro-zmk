name: Build User Config

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config-path: config
